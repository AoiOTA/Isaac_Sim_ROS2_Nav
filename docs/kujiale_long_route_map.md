# Kujiale 全屋长距离路线与避障布置（静态候选已验证）

> 配置版本：`kujiale_*_long_range.yaml` / `kujiale_long_range_campaign.yaml`
>
> 状态：本图描述当前重设计；静态候选批次 `kujiale_long_route_static_20260723-194416` 已通过 20/20，动态 20 轮尚未执行，故未完成完整 20+20 验收。旧批次报告仅是旧路线与旧障碍布局的历史证据，不能用于本布局验收。

所有坐标均为 `warehouse_new` 的 `map` 坐标。两张图由
`scripts/generate_kujiale_long_route_maps.py` 直接读取可执行配置生成，不能手工改图。

## 静态场景

![静态长距离地图与航点](figures/kujiale_long_route_static_map.png)

机器人从最下方的 G1 出生，先向 G2 行驶并通过原先 `mapping_start` 附近的中心区域。这里放置四个
`0.30 × 0.30 × 0.16 m` RGB-D 低矮方块和两个 `0.60 × 0.30 × 0.16 m` 低矮长条。完整六障碍由 GUI 在
`2026-07-23 13:37:02 +08:00` 导出：`rgbd_low_bar_east=[1.286899, 0.033996]`、
`rgbd_low_bar_north=[-0.703143, 0.252652]`、`rgbd_low_box_center=[-2.135744, -0.876959]`、
`rgbd_low_box_east=[0.366563, 0.66795]`、`rgbd_low_box_north=[0.126442, -0.579985]`、
`rgbd_low_box_west=[-0.853013, -0.961904]`；完整可编辑快照见
`isaac_sim/configs/experiments/kujiale_static_layout_draft_20260723-133702.yaml`。
这些是**已保存但仍可编辑的候选坐标**，静态 20 轮已以该基线通过；它们尚不是完整 20+20 的冻结布局。操作者可拖动六个障碍、用 RViz 多次手动发 Goal
观察绕行，再通过 `/experiment/obstacles/capture_layout` 导出精确 Map 坐标。任何变更后都必须重新生成候选地图/理论参考并复跑静态 20 轮；最终位置确认时再冻结正式实验配置。方块不会贴着 G1 出生点；进入中心区时应由深度点云、VoxelLayer 和 Costmap
形成连续可绕行的局部约束。

## 动态场景

![动态长距离地图与航点](figures/kujiale_long_route_dynamic_map.png)

动态场景不加载低矮方块。G2（出生后首个导航 Goal）被接受后，两组高 `1.0 m` 方块从 G1→G2
通道侧方进入预测行进线：

| ID | Map 轨迹 | 速度 | 延迟（含种子抖动） | 结束状态 |
|---|---|---:|---:|---|
| `g1_g2_south_crossing` | `[1.15, -1.00] → [0.45, -1.00]` | `0.10 m/s` | `G2 后 6.0 ± 0.25 s` | `hold` |
| `g1_g2_north_crossing` | `[-0.20, 0.65] → [0.50, 0.65]` | `0.10 m/s` | `G2 后 15.0 ± 0.25 s` | `hold` |

两条轨迹均为 `0.70 m`，移动时长均为 `7.0 s`。障碍不会出生时紧贴机器人、不会快速横穿整屋，也不会在碰到机器人后继续推行；运动结束后保持在通道内终点，要求导航在真正经过该行进线时观察、减速/等待或重规划绕行。

## 航点与出生点

`long_route_start_g1` 是由 `mapping_start` 的已标定 `map → USD` 变换计算并在启动契约中核验的派生出生点。它不是新的 SLAM 地图或第二套标定。

| 顺序 | 航点 | 区域标签 | `map` 坐标 `[x, y]` | 朝向 |
|---:|---|---|---:|---:|
| 0 | S / G1 | 最下方房间；出生点 | `[0.45, -5.35]` | `90°` |
| 1 | G2 | 上方大房间 | `[0.80, 4.80]` | `-160°` |
| 2 | G3 | 左上房间出口侧 | `[-2.20, 3.25]` | `-105°` |
| 3 | G4 | 左侧厕所 | `[-3.00, -0.45]` | `-68°` |
| 4 | G5 | 左下房间 | `[-2.20, -2.95]` | `-42°` |
| 5 | G1 | 最下方房间；闭环回归点 | `[0.45, -5.35]` | `90°` |

G3 和 G5 已分别移到 `[-2.20, 3.25]`、`[-2.20, -2.95]` 的高净空区域：栅格净空约 `0.70 m`、`0.75 m`，高于原位置的约 `0.47 m`、`0.46 m`；G4 沿同一通行中心线调整为 `[-3.00, -0.45]`，避免其原坐标最后约 20 cm 的终端恢复循环。它们仍依次连接 G2、G4、G1。原狭窄通道停靠点已删除，因此不会在该处要求一次单独的到点/原地转向。图中的青绿色虚线只表示 runner 的发送顺序 `S(G1) → G2 → G3 → G4 → G5 → G1`，不代表 Nav2 实际规划或 Ground Truth 路线。

G2–G5 在两图中统一使用相同的蓝色图标、尺寸和颜色；G4 仅保留普通区域标签，不使用厕所专属样式。

## 生成和核验

```bash
cd /home/lyb/Workspace/Isaac_Sim_ROS2_Nav
python3 scripts/generate_kujiale_long_route_maps.py
```

生成器会拒绝静态/动态航点不一致、路线不为重设计顺序、或 campaign 与可执行场景的航点 ID 不一致的输入。
