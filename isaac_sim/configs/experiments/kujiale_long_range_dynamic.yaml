# Schema v2 is the physical-obstacle contract for the Kujiale campaign.
# Coordinates are Map coordinates and are transformed with long_route_start_g1.
schema_version: 2
seed: 7301
enabled: false
coordinate_frame: map
spawn_pose_name: long_route_start_g1
obstacles:
  # Both actors are enabled only after G2 is accepted.  Their delayed, short
  # motions take place around the former mapping_start while the robot is in
  # transit from G1; they then hold at the endpoint rather than retiring.
  - {id: central_slow_west, mode: linear, trigger_group: G2, size: [0.40, 0.40, 1.00], mass: 20.0, start: [-0.85, 0.45, 0.50], end: [-0.55, 0.45, 0.50], speed: 0.10, delay_sec: 10.0, jitter_sec: 0.25, post_motion: hold}
  - {id: central_slow_east, mode: linear, trigger_group: G2, size: [0.40, 0.40, 1.00], mass: 20.0, start: [0.95, -0.45, 0.50], end: [0.65, -0.45, 0.50], speed: 0.08, delay_sec: 13.0, jitter_sec: 0.25, post_motion: hold}
