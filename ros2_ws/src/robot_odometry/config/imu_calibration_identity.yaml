# Explicit non-Isaac rollback profile. The raw/corrected topic split remains.
imu_yaw_calibrator:
  ros__parameters:
    use_sim_time: true
    yaw_scale: 1.0
    yaw_bias_rad_s: 0.0
    yaw_variance: 0.0001
    diagnostic_interval: 1000
