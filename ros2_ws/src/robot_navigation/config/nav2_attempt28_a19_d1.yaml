# Attempt28 A19 diagnostic D1 delta.  Only controller failure tolerance differs
# from D0; the exact through-pose tolerance is shared by every A19 profile.
planner_server:
  ros__parameters:
    GridBased:
      tolerance: 0.0

controller_server:
  ros__parameters:
    failure_tolerance: 0.6
    publish_zero_velocity: true
    controller_plugins: [FollowPath]
    progress_checker_plugins: [progress_checker]
    goal_checker_plugins: [goal_checker, a19_crossing_goal_checker]
    a19_crossing_goal_checker:
      plugin: nav2_controller::SimpleGoalChecker
      stateful: false
      xy_goal_tolerance: 0.05
      yaw_goal_tolerance: 3.141592654
