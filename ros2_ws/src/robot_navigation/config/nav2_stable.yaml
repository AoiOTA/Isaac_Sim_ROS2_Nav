controller_server:
  ros__parameters:
    controller_frequency: 20.0
    FollowPath:
      time_steps: 40
      model_dt: 0.05
      # Keep the 2 s horizon aligned with the 20 Hz command cadence.  This
      # smaller bank prevents the doubled update rate from missing deadlines.
      batch_size: 500
