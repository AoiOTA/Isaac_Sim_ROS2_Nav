# Explicit third-layer profile for the frozen stock A1 arm.  It is empty by
# design: A1 receives nav2_params.yaml plus only the stock static/dynamic
# profile selected by the runner.
controller_server:
  ros__parameters: {}
