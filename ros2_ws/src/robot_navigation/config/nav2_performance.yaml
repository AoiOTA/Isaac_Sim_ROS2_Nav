controller_server:
  ros__parameters:
    # Keep the control period equal to model_dt so MPPI sequence shifting
    # stays enabled at the 20 Hz command-chain limit.
    controller_frequency: 20.0
    FollowPath:
      time_steps: 40
      model_dt: 0.05
      batch_size: 700
