# C4 engineering scenario for the explicit V6 frozen low-obstacle profile.
schema_version: 1
scenario:
  id: v6_kujiale_low_obstacles_static
  type: static
  spawn_pose_name: long_route_start_g1
  map_version: v6_kujiale_isaacgen_v1
  posegraph_version: v6_kujiale_isaacgen_v1
  configs:
    robot: ../../../../isaac_sim/configs/robots/jackal.yaml
    nav2: ../../robot_navigation/config/nav2_params.yaml
    dynamic_obstacles: ../../../../isaac_sim/configs/experiments/v6_kujiale_low_obstacles_frozen.yaml
    optimal_reference: optimal_reference.json
  simulation: {physics_dt: 0.016666666666666666, rtf: 1.0}
  runs:
    matrix:
      - {seed: 8601, case_id: static, variant_id: v6_phase_f_r3, condition_id: v6_low_obstacles}
    timeout_sec: 600.0
    leg_timeout_sec: 180.0
  # Route completion is XY-only.  Keep yaw values for diagnostics, but do not
  # use them as a completion gate for the whole-house engineering runs.
  goal: {frame_id: map, position: [0.45, -5.35], yaw_deg: 90.0, require_orientation: false}
  route:
    - {id: G2, frame_id: map, position: [0.80, 4.80], yaw_deg: -160.0, require_orientation: false}
    - {id: G3, frame_id: map, position: [-2.20, 3.25], yaw_deg: -105.0, require_orientation: false}
    - {id: G4, frame_id: map, position: [-3.00, -0.45], yaw_deg: -68.0, require_orientation: false}
    - {id: G5, frame_id: map, position: [-2.20, -2.95], yaw_deg: -42.0, require_orientation: false}
    - {id: G1, frame_id: map, position: [0.45, -5.35], yaw_deg: 90.0, require_orientation: false}
  # Isaac contact/topic is the collision authority.  SAT overlap and its 10 mm
  # reference remain visible for diagnostics only and never set collision_detected.
  success: {position_tolerance_m: 0.25, orientation_tolerance_deg: 10.0, final_linear_speed_mps: 0.02, final_angular_speed_radps: 0.05, final_still_duration_sec: 1.0, final_still_timeout_sec: 20.0, require_safety_observations: true, minimum_ground_truth_path_length_m: 20.0, minimum_reverse_distance_m: 0.0, maximum_reverse_distance_fraction: 1.0, minimum_curved_distance_fraction: 0.0, maximum_stopped_time_fraction: 1.0, maximum_static_geometric_overlap_m: 0.010, static_geometric_overlap_is_diagnostic_only: true}
  obstacles:
    layout_id: kujiale_v6_low_obstacles_indoor_feasible_r3_20260829
    static: [{id: v6_low_box_solo}]
    trajectories: []
